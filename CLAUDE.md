- If there is work finished already, create a Git commit for it already.
Create a Git commit automatically after finishing any small, logical unit of work.
- Automatically stage untracked or modified files before committing.
- Use Conventional Commit formatting but add as much detail as possible. Use sentence case (e.g., "Feat: add login page").
- Push changes to the remote origin repository immediately after a successful commit.